import { createTheme } from "@mui/material/styles";
const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
          main: '#b5883f',
        },
        secondary: {
          main: '#f50057',
        },
      },
});

export default theme;